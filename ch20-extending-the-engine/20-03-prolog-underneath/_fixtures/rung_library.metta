;Reached through a directory registered under an alias with
;register_metta_library_path, so (library rung-alias rung_library.metta)
;resolves without the file being anywhere the engine already looks.
(= (rung-aliased $x) (+ $x 100))
